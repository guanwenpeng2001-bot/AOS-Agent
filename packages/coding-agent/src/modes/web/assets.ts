export interface WebAsset {
	readonly contentType: string;
	readonly body: string;
}

const INDEX_HTML = `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="color-scheme" content="dark light">
  <title>AOS Agent Task Graph Board</title>
  <link rel="stylesheet" href="/app.css">
</head>
<body>
  <header>
    <div><p class="eyebrow">AOS Agent Web Surface</p><h1>Task graph board</h1></div>
    <div class="header-status">
      <div id="operation-status" class="operation-status" role="status" aria-live="polite"></div>
      <div class="connection"><span id="connection-dot"></span><span id="connection-label">Connecting</span></div>
    </div>
  </header>
  <main>
    <section class="panel runs-panel">
      <div class="panel-heading"><h2>Runs</h2><span id="run-count" class="count">0</span></div>
      <div id="runs" class="list empty">No runs recorded.</div>
    </section>
    <section class="panel run-detail-panel">
      <div class="panel-heading"><h2>Run detail</h2><span id="poll-time" class="muted"></span></div>
      <div id="run-detail" class="empty-state">Select a run to inspect its status, usage, and terminal receipt.</div>
    </section>
    <section class="panel usage-panel">
      <div class="panel-heading"><h2>Usage and cost</h2><span id="usage-run-count" class="count">0 runs</span></div>
      <div id="usage-summary" class="empty-state">Loading recorded usage.</div>
    </section>
    <section class="panel gates-panel">
      <div class="panel-heading"><h2>Pending gates</h2><span id="gate-count" class="count">0</span></div>
      <div id="gates" class="gate-list empty">No gates waiting for a decision.</div>
    </section>
    <section class="panel graphs-panel">
      <div class="panel-heading"><h2>Task graphs</h2><span id="graph-count" class="count">0</span></div>
      <div id="graphs" class="graph-list empty">No task graphs recorded.</div>
    </section>
    <section class="panel board-panel">
      <div class="panel-heading"><div><h2 id="board-title">DAG</h2><span id="board-summary" class="muted"></span></div><span class="readonly">read only</span></div>
      <div id="board-warning" class="board-warning" hidden></div>
      <div class="board-grid">
        <div id="dag" class="dag empty-state">Select a graph to inspect its dependency flow.</div>
        <aside id="node-detail" class="node-detail empty-state">Select a node for availability, Attempt, Worker, and Child status.</aside>
      </div>
    </section>
    <section class="panel audit-panel">
      <div class="panel-heading"><h2>Audit query</h2><span class="readonly">read only</span></div>
      <form id="audit-form">
        <label>Run ID<input id="audit-run" name="runId" autocomplete="off" placeholder="Optional run ID"></label>
        <label>Event type<select id="audit-type" name="type"><option value="">All events</option><option>run.accepted</option><option>run.started</option><option>run.completed</option><option>run.failed</option><option>run.cancelled</option><option>run.interrupted</option><option>task.graph</option></select></label>
        <button type="submit">Query audit</button>
      </form>
      <div id="audit-results" class="audit-results empty">Submit a query to inspect safe audit events.</div>
    </section>
  </main>
  <footer>Loopback only · Confirmed Gate and Run operations · No external resources</footer>
  <script src="/app.js" defer></script>
</body>
</html>
`;

const APP_CSS = `:root{font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#e8edf4;background:#090c11;font-synthesis:none}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:radial-gradient(circle at 82% -8%,#173149 0,transparent 32%),#090c11;color:#e8edf4}header{height:88px;padding:18px 28px;border-bottom:1px solid #27303b;display:flex;align-items:center;justify-content:space-between;background:#0d1118e8;backdrop-filter:blur(16px);position:sticky;top:0;z-index:2}h1,h2,h3,p{margin:0}h1{font-size:24px;letter-spacing:-.03em}.eyebrow{text-transform:uppercase;letter-spacing:.16em;color:#7f91a6;font-size:10px;font-weight:700;margin-bottom:5px}.header-status{display:flex;align-items:center;gap:18px}.connection{display:flex;gap:9px;align-items:center;color:#aeb9c7;font-size:13px;white-space:nowrap}#connection-dot{width:8px;height:8px;border-radius:50%;background:#d59a4a;box-shadow:0 0 14px #d59a4a}.connection.online #connection-dot{background:#49c58b;box-shadow:0 0 14px #49c58b}.connection.error #connection-dot{background:#e76969;box-shadow:0 0 14px #e76969}main{display:grid;grid-template-columns:260px minmax(0,1fr);gap:16px;padding:18px 28px;max-width:1680px;margin:auto}.panel{background:#111720e6;border:1px solid #27303b;border-radius:12px;overflow:hidden;box-shadow:0 18px 50px #0004}.panel-heading{min-height:52px;padding:10px 16px;border-bottom:1px solid #27303b;display:flex;align-items:center;justify-content:space-between;gap:12px}.panel-heading h2{font-size:12px;text-transform:uppercase;letter-spacing:.09em;color:#aeb9c7}.count,.readonly{font-size:10px;color:#75869a;border:1px solid #303b48;border-radius:999px;padding:3px 8px}.readonly{text-transform:uppercase;letter-spacing:.08em}.runs-panel{grid-row:span 3;align-self:start;max-height:calc(100vh - 126px);overflow:auto}.run-detail-panel{min-height:245px}.graphs-panel{min-height:132px}.board-panel{min-height:520px}.audit-panel{grid-column:2}.list{display:flex;flex-direction:column}.run-row,.graph-card{appearance:none;border:0;border-bottom:1px solid #222b35;background:transparent;color:inherit;text-align:left;padding:13px 16px;cursor:pointer;display:grid;gap:7px}.run-row:hover,.run-row.selected,.graph-card:hover,.graph-card.selected{background:#19232e}.run-row-top,.graph-title,.node-head,.detail-heading{display:flex;justify-content:space-between;align-items:center;gap:10px}.run-id,.mono{font:600 11px ui-monospace,SFMono-Regular,Consolas,monospace;color:#d6e0eb;overflow:hidden;text-overflow:ellipsis}.status{font-size:9px;text-transform:uppercase;letter-spacing:.07em;padding:3px 7px;border-radius:999px;background:#293441;color:#aeb9c7;white-space:nowrap}.status.completed,.status.succeeded,.status.ready,.status.closed{color:#66d9a3;background:#17372c}.status.failed,.status.cancelled,.status.lost,.status.blocked{color:#ff9292;background:#3a2024}.status.running,.status.accepted,.status.active,.status.background,.status.spawning{color:#7fc6ff;background:#183248}.status.pending,.status.waiting_dependencies,.status.waiting_gate,.status.awaiting_input{color:#efc77f;background:#3a321d}.run-meta,.muted{font-size:10px;color:#75869a}.empty,.empty-state{padding:20px 16px;color:#75869a;font-size:12px}.detail-grid{padding:16px;display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:8px}.metric{background:#0b1017;border:1px solid #222c37;border-radius:8px;padding:10px;min-width:0}.metric-label{display:block;color:#718296;font-size:9px;text-transform:uppercase;letter-spacing:.08em;margin-bottom:6px}.metric-value{display:block;font-size:12px;overflow-wrap:anywhere}.detail-json{margin:0 16px 16px;padding:12px;background:#090d12;border:1px solid #222c37;border-radius:8px;overflow:auto;max-height:130px;color:#9fb2c7;font:10px/1.5 ui-monospace,SFMono-Regular,Consolas,monospace}.graph-list{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr))}.graph-card{border-right:1px solid #222b35}.graph-title strong{font:600 11px ui-monospace,SFMono-Regular,Consolas,monospace}.graph-summary{font-size:10px;color:#75869a}.board-grid{display:grid;grid-template-columns:minmax(0,1fr) 330px;min-height:465px}.dag{min-width:0;overflow:auto;border-right:1px solid #27303b;background-color:#0a0f16;background-image:linear-gradient(#18212b55 1px,transparent 1px),linear-gradient(90deg,#18212b55 1px,transparent 1px);background-size:24px 24px}.dag-svg{display:block;width:100%;min-width:700px;min-height:450px}.edge{fill:none;stroke:#405166;stroke-width:1.5}.dag-node{cursor:pointer}.dag-node rect{fill:#111a24;stroke:#3b4a5c;stroke-width:1.5}.dag-node:hover rect,.dag-node.selected rect{stroke:#91caff;stroke-width:2}.dag-node.linked rect{filter:drop-shadow(0 0 7px #4aa8ed88)}.dag-node .title{fill:#e3ebf4;font:600 12px ui-monospace,SFMono-Regular,Consolas,monospace}.dag-node .sub{fill:#8092a6;font:10px Inter,system-ui,sans-serif}.dag-node .state{font:700 9px Inter,system-ui,sans-serif;text-transform:uppercase;fill:#74d6a7}.dag-node.failed .state,.dag-node.cancelled .state{fill:#ff9292}.dag-node.pending .state{fill:#efc77f}.node-detail{padding:16px;overflow:auto;background:#0c1118}.node-detail h3{font-size:13px}.node-detail .detail-grid{grid-template-columns:1fr 1fr;padding:14px 0}.section-title{margin:16px 0 8px;color:#8294a8;font-size:9px;text-transform:uppercase;letter-spacing:.1em}.fact-list{display:grid;gap:7px}.fact{border:1px solid #26313d;background:#101821;border-radius:7px;padding:9px}.fact-top{display:flex;justify-content:space-between;gap:8px;font-size:10px}.fact-meta{color:#728498;font-size:9px;margin-top:5px;overflow-wrap:anywhere}.tree,.tree ul{list-style:none;margin:0;padding-left:0}.tree ul{margin-left:10px;padding-left:14px;border-left:1px solid #334252}.tree li{position:relative;margin:7px 0}.tree ul>li:before{content:"";position:absolute;left:-14px;top:12px;width:10px;border-top:1px solid #334252}.tree-node{border:1px solid #293643;background:#101821;border-radius:7px;padding:8px}.tree-label{display:flex;justify-content:space-between;gap:8px;font-size:10px}.tree-meta{font-size:9px;color:#728498;margin-top:4px;overflow-wrap:anywhere}.board-warning{padding:8px 16px;color:#efc77f;background:#342c18;border-bottom:1px solid #5e4d25;font-size:10px}form{padding:14px 16px;display:grid;grid-template-columns:1fr 180px auto;gap:10px;align-items:end;border-bottom:1px solid #27303b}label{display:grid;gap:6px;font-size:9px;text-transform:uppercase;letter-spacing:.08em;color:#75869a}input,select,button{height:36px;border:1px solid #344252;border-radius:7px;background:#0a1017;color:#dce5ef;padding:0 10px;font:11px inherit}form button{background:#dce8f4;color:#0a0d12;font-weight:700;cursor:pointer;padding:0 16px}form button:hover{background:#fff}.audit-results{max-height:280px;overflow:auto}.audit-row{display:grid;grid-template-columns:150px 150px 1fr;gap:10px;padding:10px 16px;border-bottom:1px solid #222b35;font-size:10px}.audit-type{color:#8fcaff}.audit-id{font-family:ui-monospace,SFMono-Regular,Consolas,monospace;color:#9fafc0;overflow:hidden;text-overflow:ellipsis}.audit-summary{color:#7f91a6;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}footer{text-align:center;color:#5f7083;font-size:10px;padding:4px 20px 24px}@media(max-width:1100px){main{grid-template-columns:220px minmax(0,1fr)}.board-grid{grid-template-columns:1fr}.dag{border-right:0;border-bottom:1px solid #27303b}.node-detail{min-height:260px}.detail-grid{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:760px){header{padding:16px}main{grid-template-columns:1fr;padding:12px 16px}.runs-panel{grid-row:auto;max-height:none}.audit-panel{grid-column:auto}form{grid-template-columns:1fr}.audit-row{grid-template-columns:1fr}.detail-grid{grid-template-columns:1fr 1fr}}
.header-status{display:flex;align-items:center;gap:18px}.operation-status{max-width:460px;color:#aeb9c7;font-size:12px;text-align:right}.operation-status.success{color:#66d9a3}.operation-status.error{color:#ff9292}.gates-panel{min-height:180px}.run-actions,.delivery-panel{margin:0 16px 16px;padding:14px;border:1px solid #293541;border-radius:9px;background:#0c1219;display:grid;gap:10px}.delivery-head{display:flex;align-items:center;justify-content:space-between;gap:10px}.delivery-head a,.claim-link{color:#8fcaff;text-decoration:none;font-size:11px}.delivery-head a:hover,.claim-link:hover{text-decoration:underline}.delivery-items{display:grid;gap:7px}.delivery-item{display:flex;align-items:center;justify-content:space-between;gap:10px;border:1px solid #26313d;border-radius:7px;padding:8px 10px;font-size:10px}.action-fields{display:grid;grid-template-columns:1fr 1fr;gap:10px}.action-buttons,.gate-actions{display:flex;gap:8px;justify-content:flex-end}.danger{border-color:#713c43;background:#3a2024;color:#ffb1b1}.danger:hover{background:#52272d}.secondary{background:#18222d;color:#c4d1de}.secondary:hover{background:#243242}.gate-list{display:flex;flex-direction:column}.gate-card{padding:13px 16px;border-bottom:1px solid #222b35;display:grid;gap:9px}.gate-head{display:flex;justify-content:space-between;gap:10px}.gate-title{font:600 12px ui-monospace,SFMono-Regular,Consolas,monospace}.gate-meta{font-size:11px;color:#75869a}.status.approved{color:#66d9a3;background:#17372c}.status.rejected{color:#ff9292;background:#3a2024}button:disabled{cursor:not-allowed;opacity:.55}.usage-panel{grid-column:2}.usage-cards{padding:16px;display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:8px;border-bottom:1px solid #27303b}.usage-breakdowns{display:grid;grid-template-columns:repeat(3,minmax(0,1fr))}.usage-breakdown{min-width:0;border-right:1px solid #27303b}.usage-breakdown:last-child{border-right:0}.usage-breakdown h3{padding:12px 14px;color:#8294a8;font-size:10px;text-transform:uppercase;letter-spacing:.08em;border-bottom:1px solid #27303b}.usage-table-wrap{overflow:auto;max-height:260px}.usage-table{width:100%;border-collapse:collapse;font-size:10px}.usage-table th,.usage-table td{padding:8px 10px;border-bottom:1px solid #222b35;text-align:right;white-space:nowrap}.usage-table th{color:#718296;font-size:9px;text-transform:uppercase;letter-spacing:.05em;background:#0c1219;position:sticky;top:0}.usage-table th:first-child,.usage-table td:first-child{text-align:left}.usage-table td:first-child{max-width:180px;overflow:hidden;text-overflow:ellipsis;font-family:ui-monospace,SFMono-Regular,Consolas,monospace;color:#c4d1de}.usage-warning{padding:8px 16px;color:#efc77f;background:#342c18;border-top:1px solid #5e4d25;font-size:10px}@media(max-width:1100px){.usage-breakdowns{grid-template-columns:1fr}.usage-breakdown{border-right:0;border-bottom:1px solid #27303b}.usage-breakdown:last-child{border-bottom:0}.usage-cards{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:760px){.usage-panel{grid-column:auto}.usage-cards{grid-template-columns:1fr 1fr}}`;

const APP_JS = `"use strict";
const SVG_NS="http://www.w3.org/2000/svg";
const state={runs:[],selectedRunId:null,board:{graphs:[],runs:[],workers:[],subagents:[],warnings:[]},selectedGraphKey:null,selectedNodeId:null,gates:[],usage:null,resumeSessionPath:"",resumeMessage:"",operationBusy:false};
const el=(id)=>document.getElementById(id);
async function callApi(path,method,params){const response=await fetch(path,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({method:method,params:params})});const payload=await response.json();if(!response.ok){const error=new Error(payload.error&&payload.error.message?payload.error.message:"RPC request failed");error.code=payload.error&&payload.error.code;throw error}return payload.data}function rpc(method,params){return callApi("/api/rpc",method,params)}function operation(method,params){return callApi("/api/ops",method,params)}function clientRequestId(prefix){return prefix+"-"+crypto.randomUUID()}
async function boardData(){const response=await fetch("/api/task-graph-board");const payload=await response.json();if(!response.ok)throw new Error(payload.error&&payload.error.message?payload.error.message:"Task graph board request failed");return payload.data}
function statusClass(value){return "status "+String(value||"unknown").replace(/[^a-z_-]/g,"")}
function graphKey(graph){return graph.taskId+":"+graph.graphRevision}
function setConnection(kind,label){const box=document.querySelector(".connection");box.className="connection "+kind;el("connection-label").textContent=label}function setOperationStatus(kind,message){const root=el("operation-status");root.className="operation-status "+kind;root.textContent=message}async function performOperation(method,params,confirmation,successMessage){if(state.operationBusy||!window.confirm(confirmation))return;state.operationBusy=true;setOperationStatus("","Working…");try{const result=await operation(method,Object.assign({},params,{confirmed:true}));setOperationStatus("success",successMessage(result));await refresh()}catch(error){setOperationStatus("error",error instanceof Error?error.message:"Operation failed")}finally{state.operationBusy=false}}function actionButton(label,className,onClick){const button=document.createElement("button");button.type="button";button.className=className;button.textContent=label;button.addEventListener("click",onClick);return button}
function selectRun(runId){state.selectedRunId=runId;for(const graph of state.board.graphs){const node=graph.nodes.find((item)=>item.runRef&&item.runRef.runId===runId);if(node){state.selectedGraphKey=graphKey(graph);state.selectedNodeId=node.nodeId;break}}renderAll()}
function renderRuns(){const root=el("runs");el("run-count").textContent=String(state.runs.length);root.replaceChildren();root.className="list";if(state.runs.length===0){root.className="list empty";root.textContent="No runs recorded.";return}state.runs.forEach((entry)=>{const run=entry.run;const button=document.createElement("button");button.type="button";button.className="run-row"+(run.id===state.selectedRunId?" selected":"");const top=document.createElement("div");top.className="run-row-top";const id=document.createElement("span");id.className="run-id";id.textContent=run.id;const badge=document.createElement("span");badge.className=statusClass(run.status);badge.textContent=run.status;top.append(id,badge);const meta=document.createElement("div");meta.className="run-meta";meta.textContent="Attempt "+run.attempt+" · "+(run.model?run.model.provider+" / "+run.model.id:"No model");button.append(top,meta);button.addEventListener("click",()=>selectRun(run.id));root.append(button)})}
function metric(label,value){const box=document.createElement("div");box.className="metric";const a=document.createElement("span");a.className="metric-label";a.textContent=label;const b=document.createElement("span");b.className="metric-value";b.textContent=value===undefined||value===null||value===""?"—":String(value);box.append(a,b);return box}
function formatTokens(value){return Number(value||0).toLocaleString("en-US")}function formatCost(value){return "$"+Number(value||0).toFixed(4)}
function usageBreakdown(title,dimensionLabel,rows,dimension){const section=document.createElement("section");section.className="usage-breakdown";const heading=document.createElement("h3");heading.textContent=title;section.append(heading);const wrap=document.createElement("div");wrap.className="usage-table-wrap";const table=document.createElement("table");table.className="usage-table";const head=document.createElement("thead");const headRow=document.createElement("tr");[dimensionLabel,"Runs","Input","Output","Total","Cost"].forEach((label)=>{const cell=document.createElement("th");cell.scope="col";cell.textContent=label;headRow.append(cell)});head.append(headRow);table.append(head);const body=document.createElement("tbody");rows.forEach((row)=>{const tr=document.createElement("tr");[dimension(row),formatTokens(row.runCount),formatTokens(row.inputTokens),formatTokens(row.outputTokens),formatTokens(row.totalTokens),formatCost(row.costUsd)].forEach((value)=>{const cell=document.createElement("td");cell.textContent=value;tr.append(cell)});body.append(tr)});if(rows.length===0){const tr=document.createElement("tr");const cell=document.createElement("td");cell.colSpan=6;cell.textContent="No recorded usage.";tr.append(cell);body.append(tr)}table.append(body);wrap.append(table);section.append(wrap);return section}
function renderUsage(){const root=el("usage-summary");root.replaceChildren();const usage=state.usage;if(!usage){root.className="empty-state";root.textContent="Loading recorded usage.";return}root.className="";el("usage-run-count").textContent=formatTokens(usage.totals.runCount)+" runs";const cards=document.createElement("div");cards.className="usage-cards";cards.append(metric("Runs",formatTokens(usage.totals.runCount)),metric("Input tokens",formatTokens(usage.totals.inputTokens)),metric("Output tokens",formatTokens(usage.totals.outputTokens)),metric("Total tokens",formatTokens(usage.totals.totalTokens)),metric("Recorded cost",formatCost(usage.totals.costUsd)));const breakdowns=document.createElement("div");breakdowns.className="usage-breakdowns";breakdowns.append(usageBreakdown("By Session","Session",usage.bySession,(row)=>row.sessionId),usageBreakdown("By model","Model",usage.byModel,(row)=>row.provider+" / "+row.modelId),usageBreakdown("By UTC day","Day",usage.byDay,(row)=>row.day));root.append(cards,breakdowns);if(usage.warningCount){const warning=document.createElement("div");warning.className="usage-warning";warning.textContent=formatTokens(usage.warningCount)+" audit warning(s) affected the source query.";root.append(warning)}}
function artifactLink(runId,artifact,label){const link=document.createElement("a");link.className="claim-link";link.textContent=label;link.href="/api/delivery/artifacts/"+encodeURIComponent(runId)+"/"+encodeURIComponent(artifact.artifactId);link.download=artifact.artifactId;return link}
function renderDelivery(entry){const panel=document.createElement("section");panel.className="delivery-panel";const head=document.createElement("div");head.className="delivery-head";const title=document.createElement("strong");title.textContent="Delivery results";head.append(title);if(entry.delivery){const pr=document.createElement("a");pr.href=entry.delivery.url;pr.target="_blank";pr.rel="noreferrer";pr.textContent=entry.delivery.repo+" #"+entry.delivery.number;head.append(pr)}panel.append(head);const items=document.createElement("div");items.className="delivery-items";if(entry.delivery){items.append(fact("PR checks",entry.delivery.conclusion,entry.delivery.checks.length+" checks · "+entry.delivery.branch));entry.delivery.checks.forEach((check)=>items.append(fact(check.name,check.conclusion||check.status,check.status)))}const receipt=entry.receipt;if(receipt&&receipt.diff){const item=document.createElement("div");item.className="delivery-item";item.append(document.createTextNode("Diff"),artifactLink(entry.run.id,receipt.diff,"Download"));items.append(item)}(receipt&&receipt.artifacts||[]).forEach((artifact)=>{const item=document.createElement("div");item.className="delivery-item";item.append(document.createTextNode(artifact.mediaType),artifactLink(entry.run.id,artifact,"Download"));items.append(item)});(receipt&&receipt.tests||[]).forEach((test)=>items.append(fact(test.name,test.status,test.summary||((test.required?"required":"optional")+" test"))));if(items.childNodes.length===0){items.className="delivery-items empty";items.textContent="No delivery facts recorded."}panel.append(items);return panel}
function renderRunDetail(){const root=el("run-detail");const entry=state.runs.find((item)=>item.run.id===state.selectedRunId);root.replaceChildren();if(!entry){root.className="empty-state";root.textContent="Select a run to inspect its status, usage, and terminal receipt.";return}root.className="";const grid=document.createElement("div");grid.className="detail-grid";const usage=entry.receipt&&entry.receipt.usage?entry.receipt.usage:{};grid.append(metric("Status",entry.run.status),metric("Attempt",entry.run.attempt),metric("Total tokens",usage.totalTokens||usage.total),metric("Input",usage.inputTokens||usage.input),metric("Output",usage.outputTokens||usage.output),metric("Terminal",entry.receipt?entry.receipt.status:"Pending"));const actions=document.createElement("div");actions.className="run-actions";const buttons=document.createElement("div");buttons.className="action-buttons";if(entry.run.status==="accepted"||entry.run.status==="running"){buttons.append(actionButton("Cancel run","danger",()=>performOperation("run.cancel",{runId:entry.run.id},"Cancel run "+entry.run.id+"?",(result)=>"Cancellation requested for "+result.runId+".")))}else{const fields=document.createElement("div");fields.className="action-fields";const pathLabel=document.createElement("label");pathLabel.textContent="Session path";const path=document.createElement("input");path.type="text";path.autocomplete="off";path.placeholder="Session file to restore";path.value=state.resumeSessionPath;path.addEventListener("input",()=>{state.resumeSessionPath=path.value});pathLabel.append(path);const messageLabel=document.createElement("label");messageLabel.textContent="Resume message";const message=document.createElement("input");message.type="text";message.autocomplete="off";message.placeholder="Instruction for the next attempt";message.value=state.resumeMessage;message.addEventListener("input",()=>{state.resumeMessage=message.value});messageLabel.append(message);fields.append(pathLabel,messageLabel);actions.append(fields);buttons.append(actionButton("Resume run","secondary",()=>{const sessionPath=state.resumeSessionPath.trim();const resumeMessage=state.resumeMessage.trim();if(!sessionPath||!resumeMessage){setOperationStatus("error","Session path and resume message are required.");return}void performOperation("run.resume",{sessionPath:sessionPath,sourceRunId:entry.run.id,message:resumeMessage,clientRequestId:clientRequestId("web-resume")},"Resume run "+entry.run.id+" as a new attempt?",(result)=>"Accepted resumed run "+result.runId+".")}))}actions.append(buttons);root.append(grid,actions,renderDelivery(entry));const pre=document.createElement("pre");pre.className="detail-json";pre.textContent=JSON.stringify(entry,null,2);root.append(pre)}
function selectGraph(graph){state.selectedGraphKey=graphKey(graph);if(!graph.nodes.some((node)=>node.nodeId===state.selectedNodeId))state.selectedNodeId=graph.nodes[0]?graph.nodes[0].nodeId:null;renderAll()}
function renderGraphs(){const graphs=state.board.graphs;const root=el("graphs");el("graph-count").textContent=String(graphs.length);root.replaceChildren();root.className="graph-list";if(graphs.length===0){root.className="graph-list empty";root.textContent="No task graphs recorded.";return}graphs.forEach((graph)=>{const card=document.createElement("button");card.type="button";card.className="graph-card"+(graphKey(graph)===state.selectedGraphKey?" selected":"");const title=document.createElement("div");title.className="graph-title";const name=document.createElement("strong");name.textContent=graph.taskId+" · r"+graph.graphRevision;const badge=document.createElement("span");badge.className=statusClass(graph.summary.status);badge.textContent=graph.summary.status;title.append(name,badge);const summary=document.createElement("div");summary.className="graph-summary";summary.textContent=graph.nodes.length+" nodes · "+graph.summary.running+" running · "+graph.summary.failed+" failed";card.append(title,summary);card.addEventListener("click",()=>selectGraph(graph));root.append(card)})}
function svgElement(name,attributes){const node=document.createElementNS(SVG_NS,name);Object.entries(attributes||{}).forEach(([key,value])=>node.setAttribute(key,String(value)));return node}
function graphLayout(graph){const byId=new Map(graph.nodes.map((node)=>[node.nodeId,node]));const depths=new Map();function depth(nodeId,visiting){if(depths.has(nodeId))return depths.get(nodeId);if(visiting.has(nodeId))return 0;const node=byId.get(nodeId);if(!node)return 0;const next=new Set(visiting);next.add(nodeId);const value=node.dependsOn.length===0?0:Math.max(...node.dependsOn.map((id)=>depth(id,next)))+1;depths.set(nodeId,value);return value}graph.nodes.forEach((node)=>depth(node.nodeId,new Set()));const levels=[];graph.nodes.forEach((node)=>{const index=depths.get(node.nodeId)||0;if(!levels[index])levels[index]=[];levels[index].push(node)});levels.forEach((level)=>level.sort((a,b)=>a.nodeId.localeCompare(b.nodeId)));const width=Math.max(720,levels.length*240+60);const maxRows=Math.max(1,...levels.map((level)=>level.length));const height=Math.max(430,maxRows*104+70);const positions=new Map();levels.forEach((level,column)=>{const block=level.length*104-24;const start=(height-block)/2;level.forEach((node,row)=>positions.set(node.nodeId,{x:36+column*240,y:start+row*104}))});return {width:width,height:height,positions:positions}}
function renderDag(graph){const root=el("dag");root.replaceChildren();root.className="dag";if(graph.nodes.length===0){root.className="dag empty-state";root.textContent="This graph has no nodes.";return}const layout=graphLayout(graph);const svg=svgElement("svg",{class:"dag-svg",viewBox:"0 0 "+layout.width+" "+layout.height,"aria-label":"Task graph dependency DAG",role:"img"});const defs=svgElement("defs");const marker=svgElement("marker",{id:"arrow",viewBox:"0 0 10 10",refX:"9",refY:"5",markerWidth:"6",markerHeight:"6",orient:"auto-start-reverse"});marker.append(svgElement("path",{d:"M 0 0 L 10 5 L 0 10 z",fill:"#405166"}));defs.append(marker);svg.append(defs);graph.nodes.forEach((node)=>{const target=layout.positions.get(node.nodeId);node.dependsOn.forEach((dependencyId)=>{const source=layout.positions.get(dependencyId);if(!source||!target)return;const startX=source.x+178;const startY=source.y+34;const endX=target.x-8;const endY=target.y+34;const bend=(startX+endX)/2;svg.append(svgElement("path",{class:"edge",d:"M "+startX+" "+startY+" C "+bend+" "+startY+", "+bend+" "+endY+", "+endX+" "+endY,"marker-end":"url(#arrow)"}))})});graph.nodes.forEach((node)=>{const position=layout.positions.get(node.nodeId);const group=svgElement("g",{class:"dag-node "+node.status+(node.nodeId===state.selectedNodeId?" selected":"")+(node.runRef&&node.runRef.runId===state.selectedRunId?" linked":""),transform:"translate("+position.x+" "+position.y+")",tabindex:"0",role:"button","aria-label":"Select node "+node.nodeId});group.append(svgElement("rect",{width:"178",height:"68",rx:"9"}));const title=svgElement("title");title.textContent=node.nodeId;group.append(title);const label=svgElement("text",{x:"12",y:"21",class:"title"});label.textContent=node.nodeId.length>22?node.nodeId.slice(0,21)+"…":node.nodeId;const status=svgElement("text",{x:"12",y:"42",class:"state"});status.textContent=node.status;const availability=svgElement("text",{x:"12",y:"57",class:"sub"});availability.textContent=node.availability||"terminal";group.append(label,status,availability);const choose=()=>{state.selectedNodeId=node.nodeId;if(node.runRef)state.selectedRunId=node.runRef.runId;renderAll()};group.addEventListener("click",choose);group.addEventListener("keydown",(event)=>{if(event.key==="Enter"||event.key===" "){event.preventDefault();choose()}});svg.append(group)});root.append(svg)}
function fact(title,status,meta){const box=document.createElement("div");box.className="fact";const top=document.createElement("div");top.className="fact-top";const name=document.createElement("span");name.className="mono";name.textContent=title;const badge=document.createElement("span");badge.className=statusClass(status);badge.textContent=status;top.append(name,badge);const detail=document.createElement("div");detail.className="fact-meta";detail.textContent=meta;box.append(top,detail);return box}
function treeItem(label,status,meta){const li=document.createElement("li");const box=document.createElement("div");box.className="tree-node";const top=document.createElement("div");top.className="tree-label";const name=document.createElement("span");name.className="mono";name.textContent=label;const badge=document.createElement("span");badge.className=statusClass(status);badge.textContent=status;top.append(name,badge);const detail=document.createElement("div");detail.className="tree-meta";detail.textContent=meta;box.append(top,detail);li.append(box);return li}
function renderLineage(root,runId,subagents){root.replaceChildren();if(subagents.length===0){root.className="fact-list empty";root.textContent="No Child Agent records for this Run.";return}root.className="tree";const childrenByParent=new Map();const childIds=new Set(subagents.map((item)=>item.childAgentInstanceId));subagents.forEach((item)=>{const children=childrenByParent.get(item.parentAgentInstanceId)||[];children.push(item);childrenByParent.set(item.parentAgentInstanceId,children)});let roots=[...childrenByParent.keys()].filter((id)=>!childIds.has(id));if(roots.length===0)roots=[subagents[0].parentAgentInstanceId];const seen=new Set();function appendChildren(parentId,parentElement){const list=childrenByParent.get(parentId)||[];if(list.length===0)return;const ul=document.createElement("ul");list.forEach((item)=>{if(seen.has(item.childAgentInstanceId))return;seen.add(item.childAgentInstanceId);const li=treeItem(item.childAgentInstanceId,item.status,item.providerKind+" · Attempt "+item.correlation.attemptId+" · "+item.safeSummary);appendChildren(item.childAgentInstanceId,li);ul.append(li)});parentElement.append(ul)}roots.forEach((parentId)=>{const li=treeItem(parentId,"root","Run "+runId);appendChildren(parentId,li);root.append(li)})}
function renderNodeDetail(graph){const root=el("node-detail");root.replaceChildren();const node=graph.nodes.find((item)=>item.nodeId===state.selectedNodeId);if(!node){root.className="node-detail empty-state";root.textContent="Select a node for availability, Attempt, Worker, and Child status.";return}root.className="node-detail";const heading=document.createElement("div");heading.className="detail-heading";const title=document.createElement("h3");title.textContent=node.nodeId;const badge=document.createElement("span");badge.className=statusClass(node.status);badge.textContent=node.status;heading.append(title,badge);const grid=document.createElement("div");grid.className="detail-grid";grid.append(metric("Availability",node.availability||"terminal"),metric("Revision",node.nodeRevision),metric("Gate",node.gateStatus||"none"),metric("Blocking",node.blockingNodeIds.length?node.blockingNodeIds.join(", "):"none"));root.append(heading,grid);const runId=node.runRef&&node.runRef.runId;const run=state.board.runs.find((entry)=>entry.run.id===runId);const workers=state.board.workers.filter((worker)=>worker.runId===runId);const subagents=state.board.subagents.filter((item)=>item.runId===runId);const attemptTitle=document.createElement("div");attemptTitle.className="section-title";attemptTitle.textContent="Attempt and Worker status";root.append(attemptTitle);const facts=document.createElement("div");facts.className="fact-list";if(run)facts.append(fact(run.run.id,run.run.status,"Attempt "+run.run.attempt+" · "+(run.receipt?"terminal receipt "+run.receipt.status:"receipt pending")));workers.forEach((worker)=>facts.append(fact(worker.workerId,worker.status,"Attempt "+(worker.attemptId||"unbound")+" · "+worker.providerId+" · lane "+worker.laneId)));if(!run&&workers.length===0){facts.className="fact-list empty";facts.textContent=runId?"Run or Worker projection is unavailable.":"No Run is attached to this node."}root.append(facts);const childTitle=document.createElement("div");childTitle.className="section-title";childTitle.textContent="Parent / Child tree";const tree=document.createElement("ul");root.append(childTitle,tree);renderLineage(tree,runId||"unattached",subagents)}
function renderBoard(){const graph=state.board.graphs.find((item)=>graphKey(item)===state.selectedGraphKey);const warning=el("board-warning");warning.hidden=state.board.warnings.length===0;warning.textContent=state.board.warnings.length?"Partial projection: "+state.board.warnings.join(", "):"";if(!graph){el("board-title").textContent="DAG";el("board-summary").textContent="";el("dag").replaceChildren();el("dag").className="dag empty-state";el("dag").textContent="Select a graph to inspect its dependency flow.";renderNodeDetail({nodes:[]});return}el("board-title").textContent=graph.taskId+" · revision "+graph.graphRevision;el("board-summary").textContent=graph.summary.pending+" pending · "+graph.summary.running+" running · "+graph.summary.succeeded+" succeeded · "+graph.summary.failed+" failed";renderDag(graph);renderNodeDetail(graph)}
function renderGates(){const gates=state.gates;const root=el("gates");el("gate-count").textContent=String(gates.length);root.replaceChildren();root.className="gate-list";if(gates.length===0){root.className="gate-list empty";root.textContent="No gates waiting for a decision.";return}gates.forEach((gate)=>{const card=document.createElement("article");card.className="gate-card";const head=document.createElement("div");head.className="gate-head";const title=document.createElement("span");title.className="gate-title";title.textContent=gate.taskId+" · "+gate.stageId;const badge=document.createElement("span");badge.className=statusClass(gate.status);badge.textContent=gate.status;head.append(title,badge);const meta=document.createElement("div");meta.className="gate-meta";meta.textContent="Revision "+gate.stageRevision+(gate.runId?" · Run "+gate.runId:"");const actions=document.createElement("div");actions.className="gate-actions";actions.append(actionButton("Reject","danger",()=>performOperation("task.gate.reject",{gateId:gate.gateId,clientRequestId:clientRequestId("web-gate-reject")},"Reject gate "+gate.gateId+"?",(result)=>"Rejected gate "+result.gate.gateId+".")),actionButton("Approve","secondary",()=>performOperation("task.gate.approve",{gateId:gate.gateId,clientRequestId:clientRequestId("web-gate-approve")},"Approve gate "+gate.gateId+"?",(result)=>"Approved gate "+result.gate.gateId+".")));card.append(head,meta,actions);root.append(card)})}
function renderAll(){renderRuns();renderRunDetail();renderUsage();renderGates();renderGraphs();renderBoard()}
function auditRunIds(events){const ids=[];events.forEach((event)=>{if(event.runId&&!ids.includes(event.runId))ids.push(event.runId)});return ids}
async function refresh(){try{const [audit,board,gates,usage]=await Promise.all([rpc("audit.query",{scope:"current-session",types:["run.accepted","run.started","run.completed","run.failed","run.cancelled","run.interrupted"],limit:100}),boardData(),rpc("task.gate.list",{status:"pending",limit:100}),rpc("usage.summary",{})]);const ids=auditRunIds(audit.events);const auditRuns=await Promise.all(ids.map((id)=>rpc("run.get",{runId:id})));const byId=new Map();[...auditRuns,...board.runs].forEach((entry)=>byId.set(entry.run.id,entry));state.runs=[...byId.values()];state.board=board;state.gates=gates.gates;state.usage=usage;if(!board.graphs.some((item)=>graphKey(item)===state.selectedGraphKey))state.selectedGraphKey=board.graphs[0]?graphKey(board.graphs[0]):null;const graph=board.graphs.find((item)=>graphKey(item)===state.selectedGraphKey);if(graph&&!graph.nodes.some((node)=>node.nodeId===state.selectedNodeId))state.selectedNodeId=graph.nodes[0]?graph.nodes[0].nodeId:null;if(!state.selectedRunId&&state.runs.length)state.selectedRunId=state.runs[0].run.id;renderAll();el("poll-time").textContent="Updated "+new Date().toLocaleTimeString();setConnection("online","Connected")}catch(error){setConnection("error",error instanceof Error?error.message:"Connection failed")}}
function renderAudit(result){const root=el("audit-results");root.replaceChildren();root.className="audit-results";if(result.events.length===0){root.className="audit-results empty";root.textContent="No matching audit events.";return}result.events.forEach((event)=>{const row=document.createElement("div");row.className="audit-row";const type=document.createElement("span");type.className="audit-type";type.textContent=event.type;const id=document.createElement("span");id.className="audit-id";id.textContent=event.runId||event.eventId;const summary=document.createElement("span");summary.className="audit-summary";summary.textContent=JSON.stringify(event.summary||{});row.append(type,id,summary);root.append(row)})}
el("audit-form").addEventListener("submit",async(event)=>{event.preventDefault();const query={scope:"current-session",limit:100};const runId=el("audit-run").value.trim();const type=el("audit-type").value;if(runId)query.runId=runId;if(type)query.types=[type];try{renderAudit(await rpc("audit.query",query))}catch(error){el("audit-results").textContent=error instanceof Error?error.message:"Audit query failed"}});
refresh();setInterval(refresh,3000);
`;

export const WEB_ASSETS: Readonly<Record<string, WebAsset>> = Object.freeze({
	"/": { contentType: "text/html; charset=utf-8", body: INDEX_HTML },
	"/index.html": { contentType: "text/html; charset=utf-8", body: INDEX_HTML },
	"/app.css": { contentType: "text/css; charset=utf-8", body: APP_CSS },
	"/app.js": { contentType: "text/javascript; charset=utf-8", body: APP_JS },
});
