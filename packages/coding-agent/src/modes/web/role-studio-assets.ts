import type { WebAsset } from "./assets.ts";

const ROLE_STUDIO_HTML = `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="color-scheme" content="dark light">
  <title>AOS Agent Role Studio</title>
  <link rel="stylesheet" href="/role-studio.css">
</head>
<body>
  <header><div><p> AOS Agent Web Surface</p><h1>Role / Mode Studio</h1></div><nav><a href="/">Task graph board</a><span id="status" role="status">Connecting</span></nav></header>
  <main>
    <aside class="panel roles">
      <div class="heading"><h2>Roles</h2><button id="new-role" type="button">New</button></div>
      <div class="filters"><input id="search" placeholder="Search roles"><select id="scope-filter"><option value="">All scopes</option><option>global</option><option>project</option></select></div>
      <div id="role-list" class="list empty">No roles.</div>
    </aside>
    <section class="panel editor">
      <div class="heading"><div><h2 id="editor-title">Create role</h2><small id="role-source">Unsaved</small></div><div class="actions"><button id="copy-role" type="button">Copy</button><button id="delete-role" class="danger" type="button">Delete</button><button id="save-role" class="primary" type="button">Save role</button></div></div>
      <form id="role-form">
        <label>Role ID<input id="role-id" required></label><label>Scope<select id="role-scope"><option>global</option><option>project</option></select></label>
        <label>Slug<input id="role-slug" required></label><label>Name<input id="role-name"></label>
        <label class="wide">Description<textarea id="role-description"></textarea></label>
        <label class="wide">When to use<textarea id="role-when"></textarea></label>
        <label class="wide">Persona<textarea id="role-persona"></textarea></label>
        <label class="wide">Custom instructions<textarea id="role-instructions"></textarea></label>
        <label>Model profile<select id="role-model"></select></label><label>Execution policy<input id="role-policy" placeholder="Active policy or reference ID"></label>
        <fieldset><legend>Capabilities</legend><select id="cap-policy"><option>all</option><option>none</option><option>named</option><option>except</option></select><input id="cap-names" placeholder="Comma-separated IDs"></fieldset>
        <fieldset><legend>Skills</legend><select id="skill-policy"><option>all</option><option>none</option><option>named</option><option>except</option></select><input id="skill-names" placeholder="Comma-separated IDs"></fieldset>
        <fieldset><legend>MCP servers</legend><select id="mcp-policy"><option>none</option><option>all</option><option>named</option><option>except</option></select><input id="mcp-names" placeholder="Comma-separated server IDs"></fieldset>
        <fieldset><legend>Managed permission ceiling</legend><select id="parent-policy"><option>all</option><option>none</option><option>named</option><option>except</option></select><input id="parent-names" placeholder="Comma-separated allowed IDs"></fieldset>
      </form>
    </section>
    <section class="panel profiles">
      <div class="heading"><div><h2>Model profiles</h2><small>Secret-free immutable revisions</small></div><button id="new-profile" type="button">New</button></div>
      <div class="profile-grid"><div id="profile-list" class="list empty">No profiles.</div><form id="profile-form"><label>ID<input id="profile-id" required></label><label>Name<input id="profile-name"></label><label>Provider<input id="profile-provider" required></label><label>Model<input id="profile-model" required></label><label>Effort<input id="profile-effort"></label><label>Service tier<input id="profile-tier"></label><label>Token budget<input id="profile-tokens" type="number" min="0"></label><button id="save-profile" class="primary" type="submit">Save profile</button></form></div>
    </section>
    <section class="panel preview">
      <div class="heading"><div><h2>Resolved AgentBinding preview</h2><small>Read-only calculation through the production Resolver</small></div><button id="refresh-preview" type="button">Refresh</button></div>
      <div id="permission" class="permission">Complete the role and select a ModelProfile.</div><pre id="binding-preview">No preview.</pre>
    </section>
  </main>
  <footer>Loopback only · Every Role/Profile write requires confirmation · Preview has no side effects</footer>
  <script src="/role-studio.js" defer></script>
</body>
</html>`;

const ROLE_STUDIO_CSS = `:root{font-family:Inter,system-ui,sans-serif;color:#e8edf4;background:#090c11}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 80% -10%,#18344d,transparent 35%),#090c11;min-height:100vh}header{height:82px;padding:16px 28px;border-bottom:1px solid #27303b;display:flex;justify-content:space-between;align-items:center;background:#0d1118ee;position:sticky;top:0;z-index:2}h1,h2,p{margin:0}header p{font-size:10px;text-transform:uppercase;letter-spacing:.15em;color:#7f91a6}h1{font-size:23px}nav{display:flex;gap:18px;align-items:center;font-size:12px}a{color:#8fcaff}#status{color:#8fa0b3}main{display:grid;grid-template-columns:270px minmax(500px,1fr);gap:16px;padding:18px 28px;max-width:1600px;margin:auto}.panel{background:#111720e8;border:1px solid #27303b;border-radius:12px;overflow:hidden}.roles{grid-row:span 3;align-self:start;max-height:calc(100vh - 120px);overflow:auto}.heading{min-height:54px;padding:10px 14px;border-bottom:1px solid #27303b;display:flex;align-items:center;justify-content:space-between;gap:12px}.heading h2{font-size:12px;text-transform:uppercase;letter-spacing:.09em;color:#b3c0ce}.heading small{color:#728397}.actions,.filters{display:flex;gap:8px}.filters{padding:10px;border-bottom:1px solid #27303b}.filters input{min-width:0}.list{display:flex;flex-direction:column}.empty{padding:18px;color:#75869a}.role-row,.profile-row{border:0;border-bottom:1px solid #222b35;background:transparent;color:inherit;text-align:left;padding:12px 14px;cursor:pointer}.role-row:hover,.role-row.selected,.profile-row:hover,.profile-row.selected{background:#19232e}.row-top{display:flex;justify-content:space-between;gap:8px}.mono{font:600 11px ui-monospace,Consolas,monospace}.badge{font-size:9px;text-transform:uppercase;color:#8fcaff}.meta{font-size:10px;color:#74869a;margin-top:5px}button,input,select,textarea{border:1px solid #344252;border-radius:7px;background:#0a1017;color:#dce5ef;padding:8px 10px;font:11px inherit}button{cursor:pointer;height:34px}button:hover{border-color:#71869c}.primary{background:#dce8f4;color:#0a0d12;font-weight:700}.danger{border-color:#713c43;color:#ffaaaa;background:#321c21}.editor form{padding:14px;display:grid;grid-template-columns:1fr 1fr;gap:12px}label{display:grid;gap:6px;font-size:9px;text-transform:uppercase;letter-spacing:.08em;color:#7d8ea1}.wide{grid-column:1/-1}textarea{min-height:64px;resize:vertical}fieldset{margin:0;border:1px solid #293642;border-radius:8px;padding:10px;display:grid;grid-template-columns:120px 1fr;gap:8px}legend{font-size:9px;text-transform:uppercase;color:#8da0b3}.profile-grid{display:grid;grid-template-columns:270px 1fr}.profile-grid form{padding:14px;display:grid;grid-template-columns:1fr 1fr;gap:10px}.profile-grid form button{align-self:end}.preview{min-height:390px}.permission{margin:14px;padding:11px;border:1px solid #31404f;border-radius:8px;color:#9fb2c7;font-size:12px}.permission.ok{border-color:#24563f;color:#71d3a4}.permission.error{border-color:#713c43;color:#ff9b9b}.preview pre{margin:0 14px 14px;padding:14px;background:#090d12;border:1px solid #222c37;border-radius:8px;overflow:auto;max-height:420px;color:#9fb2c7;font:10px/1.5 ui-monospace,Consolas,monospace}footer{text-align:center;color:#617286;font-size:10px;padding:0 20px 22px}@media(max-width:900px){main{grid-template-columns:1fr;padding:12px}.roles{grid-row:auto;max-height:none}.editor form,.profile-grid,.profile-grid form{grid-template-columns:1fr}.wide{grid-column:auto}}`;

const ROLE_STUDIO_JS = `"use strict";
const state={roles:[],profiles:[],selectedRole:null,selectedProfile:null,policy:null,busy:false};
const el=(id)=>document.getElementById(id);
async function api(path,method,params){const response=await fetch(path,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({method:method,params:params||{}})});const payload=await response.json();if(!response.ok)throw new Error(payload.error&&payload.error.message||"Request failed");return payload.data}
const read=(method,params)=>api("/api/role-studio/rpc",method,params);
async function write(method,params,message){if(state.busy||!window.confirm(message))return;state.busy=true;setStatus("Working");try{const result=await api("/api/role-studio/ops",method,Object.assign({},params,{confirmed:true}));await load();setStatus("Saved");return result}catch(error){setStatus(error instanceof Error?error.message:"Write failed",true)}finally{state.busy=false}}
function setStatus(message,error){el("status").textContent=message;el("status").style.color=error?"#ff9292":""}
function names(id){return el(id).value.split(",").map((item)=>item.trim()).filter(Boolean)}
function selector(policyId,namesId){const policy=el(policyId).value;return policy==="all"||policy==="none"?{policy:policy}:{policy:policy,named:names(namesId)}}
function setSelector(prefix,value){el(prefix+"-policy").value=value.policy;el(prefix+"-names").value=(value.named||[]).join(", ")}
function versioned(type,id,revision){const value={schemaVersion:1,type:type,id:id};if(Number.isSafeInteger(revision))value.revision=revision;return value}
function roleDefinition(){const profile=state.profiles.find((item)=>item.modelProfileId===el("role-model").value);if(!profile)throw new Error("Select a ModelProfile");const policyId=el("role-policy").value.trim();const definition={schemaVersion:1,roleId:el("role-id").value.trim(),scope:el("role-scope").value,slug:el("role-slug").value.trim(),name:el("role-name").value,description:el("role-description").value,revision:state.selectedRole?state.selectedRole.definition.revision:0,persona:el("role-persona").value,modelProfileRef:versioned("model_profile",profile.modelProfileId,profile.currentRevision.revision),capabilitySelector:selector("cap-policy","cap-names"),skillSelector:selector("skill-policy","skill-names"),mcpSelector:selector("mcp-policy","mcp-names")};const when=el("role-when").value;const instructions=el("role-instructions").value;if(when)definition.whenToUse=when;if(instructions)definition.customInstructions=instructions;if(policyId)definition.executionPolicyRef=versioned("execution_policy",policyId);return definition}
function clearRole(){state.selectedRole=null;el("role-form").reset();el("role-scope").value="global";el("cap-policy").value="all";el("skill-policy").value="all";el("mcp-policy").value="none";el("parent-policy").value="all";el("role-policy").value=state.policy?state.policy.summary.profileId:"";el("editor-title").textContent="Create role";el("role-source").textContent="Unsaved";el("role-id").disabled=false;el("role-scope").disabled=false;el("role-slug").disabled=false;renderRoles();void preview()}
function selectRole(record){state.selectedRole=record;const d=record.definition;el("role-id").value=d.roleId;el("role-scope").value=d.scope;el("role-slug").value=d.slug;el("role-name").value=d.name;el("role-description").value=d.description;el("role-when").value=d.whenToUse||"";el("role-persona").value=d.persona;el("role-instructions").value=d.customInstructions||"";el("role-model").value=d.modelProfileRef.id;el("role-policy").value=d.executionPolicyRef?d.executionPolicyRef.id:(state.policy?state.policy.summary.profileId:"");setSelector("cap",d.capabilitySelector);setSelector("skill",d.skillSelector);setSelector("mcp",d.mcpSelector);el("editor-title").textContent=d.name||d.roleId;el("role-source").textContent=d.scope+" · revision "+record.currentRevision.revision+" · "+record.revisions.length+" immutable versions";el("role-id").disabled=true;el("role-scope").disabled=true;el("role-slug").disabled=true;renderRoles();void preview()}
function renderRoles(){const root=el("role-list");root.replaceChildren();const query=el("search").value.toLowerCase();const scope=el("scope-filter").value;const rows=state.roles.filter((record)=>(!scope||record.scope===scope)&&(!query||[record.roleId,record.definition.name,record.definition.description].join(" ").toLowerCase().includes(query)));if(!rows.length){root.className="list empty";root.textContent="No matching roles.";return}root.className="list";rows.forEach((record)=>{const button=document.createElement("button");button.className="role-row"+(state.selectedRole&&state.selectedRole.roleId===record.roleId&&state.selectedRole.scope===record.scope?" selected":"");button.type="button";const top=document.createElement("div");top.className="row-top";const id=document.createElement("span");id.className="mono";id.textContent=record.roleId;const badge=document.createElement("span");badge.className="badge";badge.textContent=record.scope;top.append(id,badge);const meta=document.createElement("div");meta.className="meta";meta.textContent=(record.definition.name||"Unnamed")+" · r"+record.currentRevision.revision;button.append(top,meta);button.addEventListener("click",()=>selectRole(record));root.append(button)})}
function renderProfiles(){const list=el("profile-list");const select=el("role-model");list.replaceChildren();select.replaceChildren();state.profiles.forEach((record)=>{const option=document.createElement("option");option.value=record.modelProfileId;option.textContent=record.currentRevision.name||record.modelProfileId;select.append(option);const button=document.createElement("button");button.type="button";button.className="profile-row"+(state.selectedProfile&&state.selectedProfile.modelProfileId===record.modelProfileId?" selected":"");button.textContent=(record.currentRevision.name||record.modelProfileId)+" · "+record.currentRevision.provider+"/"+record.currentRevision.model+" · r"+record.currentRevision.revision;button.addEventListener("click",()=>selectProfile(record));list.append(button)});if(!state.profiles.length){list.className="list empty";list.textContent="No ModelProfiles."}else list.className="list";if(state.selectedRole)select.value=state.selectedRole.definition.modelProfileRef.id}
function selectProfile(record){state.selectedProfile=record;const p=record.currentRevision;el("profile-id").value=p.modelProfileId;el("profile-id").disabled=true;el("profile-name").value=p.name||"";el("profile-provider").value=p.provider;el("profile-model").value=p.model;el("profile-effort").value=p.effort||"";el("profile-tier").value=p.serviceTier||"";el("profile-tokens").value=p.budget.tokens===undefined?"":String(p.budget.tokens);renderProfiles()}
function clearProfile(){state.selectedProfile=null;el("profile-form").reset();el("profile-id").disabled=false;renderProfiles()}
async function preview(){try{const definition=roleDefinition();const profile=state.profiles.find((item)=>item.modelProfileId===definition.modelProfileRef.id).currentRevision;const result=await read("role.preview",{definition:definition,modelProfile:profile,parentCapabilitySelector:selector("parent-policy","parent-names")});const permission=el("permission");permission.className="permission "+(result.permission.tightens?"ok":"error");permission.textContent=result.permission.tightens?"Tighten-only check passed.":"Permission widening blocked: "+result.permission.reason;el("binding-preview").textContent=result.resolution?JSON.stringify({binding:result.resolution.binding,sources:result.resolution.sourceTrace,conflicts:result.resolution.conflicts},null,2):"Binding is unavailable because permissions widen the managed ceiling."}catch(error){el("permission").className="permission error";el("permission").textContent=error instanceof Error?error.message:"Preview failed";el("binding-preview").textContent="No preview."}}
async function load(){try{const results=await Promise.all([read("role.list",{includeTombstones:false}),read("model_profile.list",{}),read("policy.get",{})]);state.roles=results[0].records;state.profiles=results[1].records;state.policy=results[2];renderProfiles();if(state.selectedRole){const updated=state.roles.find((item)=>item.roleId===state.selectedRole.roleId&&item.scope===state.selectedRole.scope);if(updated)selectRole(updated);else clearRole()}else renderRoles();setStatus("Connected")}catch(error){setStatus(error instanceof Error?error.message:"Connection failed",true)}}
el("new-role").addEventListener("click",clearRole);el("new-profile").addEventListener("click",clearProfile);el("search").addEventListener("input",renderRoles);el("scope-filter").addEventListener("change",renderRoles);el("refresh-preview").addEventListener("click",()=>void preview());
el("save-role").addEventListener("click",async()=>{try{const definition=roleDefinition();if(state.selectedRole){const patch={name:definition.name,description:definition.description,whenToUse:definition.whenToUse||"",persona:definition.persona,customInstructions:definition.customInstructions||"",modelProfileRef:definition.modelProfileRef,capabilitySelector:definition.capabilitySelector,skillSelector:definition.skillSelector,mcpSelector:definition.mcpSelector};if(definition.executionPolicyRef)patch.executionPolicyRef=definition.executionPolicyRef;const saved=await write("role.edit",{roleId:state.selectedRole.roleId,scope:state.selectedRole.scope,expectedRevision:state.selectedRole.currentRevision.revision,patch:patch},"Save a new immutable revision of "+state.selectedRole.roleId+"?");if(saved)state.selectedRole=saved}else{const saved=await write("role.create",{definition:definition},"Create Role "+definition.roleId+"?");if(saved)state.selectedRole=saved}}catch(error){setStatus(error instanceof Error?error.message:"Role is invalid",true)}});
el("copy-role").addEventListener("click",async()=>{if(!state.selectedRole)return;const target=window.prompt("Target Role ID");if(!target)return;const saved=await write("role.copy",{sourceRoleId:state.selectedRole.roleId,sourceScope:state.selectedRole.scope,targetRoleId:target.trim(),targetScope:state.selectedRole.scope,expectedRevision:state.selectedRole.currentRevision.revision},"Copy "+state.selectedRole.roleId+" to "+target.trim()+"?");if(saved)state.selectedRole=saved});
el("delete-role").addEventListener("click",async()=>{if(!state.selectedRole)return;await write("role.delete",{roleId:state.selectedRole.roleId,scope:state.selectedRole.scope,expectedRevision:state.selectedRole.currentRevision.revision,reason:"deleted_from_role_studio"},"Delete Role "+state.selectedRole.roleId+"? Its immutable history remains as a tombstone.");state.selectedRole=null;clearRole()});
el("profile-form").addEventListener("submit",async(event)=>{event.preventDefault();const tokens=el("profile-tokens").value;const profile={schemaVersion:1,modelProfileId:el("profile-id").value.trim(),name:el("profile-name").value,provider:el("profile-provider").value.trim(),model:el("profile-model").value.trim(),budget:tokens?{tokens:Number(tokens)}:{}};if(el("profile-effort").value.trim())profile.effort=el("profile-effort").value.trim();if(el("profile-tier").value.trim())profile.serviceTier=el("profile-tier").value.trim();const saved=await write("model_profile.put",Object.assign({profile:profile},state.selectedProfile?{expectedRevision:state.selectedProfile.currentRevision.revision}:{}),state.selectedProfile?"Create a new revision of "+profile.modelProfileId+"?":"Create ModelProfile "+profile.modelProfileId+"?");if(saved)state.selectedProfile=state.profiles.find((item)=>item.modelProfileId===saved.modelProfileId)||null});
["role-model","role-policy","cap-policy","cap-names","skill-policy","skill-names","mcp-policy","mcp-names","parent-policy","parent-names"].forEach((id)=>el(id).addEventListener("change",()=>void preview()));
load();`;

export const ROLE_STUDIO_ASSETS: Readonly<Record<string, WebAsset>> = Object.freeze({
	"/role-studio": { contentType: "text/html; charset=utf-8", body: ROLE_STUDIO_HTML },
	"/role-studio.html": { contentType: "text/html; charset=utf-8", body: ROLE_STUDIO_HTML },
	"/role-studio.css": { contentType: "text/css; charset=utf-8", body: ROLE_STUDIO_CSS },
	"/role-studio.js": { contentType: "text/javascript; charset=utf-8", body: ROLE_STUDIO_JS },
});
